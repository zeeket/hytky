# Google OAuth 2.0 Credentials
# Get these from Google Cloud Console -> APIs & Services -> Credentials
# Create OAuth 2.0 Client ID (type: Desktop app)
GOOGLE_CLIENT_ID=xxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=xxxxx

# Google Refresh Token
# Generate this by running: cd gcalservice && npm run setup-oauth
GOOGLE_REFRESH_TOKEN=xxxxx

# Google Calendar Configuration
GOOGLE_CALENDAR_ID=your-calendar-id@group.calendar.google.com

# Sync Configuration
SYNC_INTERVAL_MINUTES=15

# Main app URL (Docker network)
# Use service name 'dev' and port 80 for docker-compose.dev.yml
# Use service name 'hytky' and port 3000 for production docker-compose
MAIN_APP_URL=http://dev:80

# Service port
PORT=3002

# Environment
NODE_ENV=production
